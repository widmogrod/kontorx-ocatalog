# Rewrite rules for Rapide_Router_Rewrite

SetEnv BOOTSTRAP development
SetEnv CATALOG_TYPE newlekarze

RewriteEngine On

RewriteBase /

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule (.*) index.php [L]
